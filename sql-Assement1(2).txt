1. Write a query to insert a new employee into the Employees table with values (1,'John', 'HR', 50000).
  
Query:
use smvec;
create table Employees (id INT PRIMARY KEY, name VARCHAR(50),department VARCHAR(50),salary DECIMAL(10,2));
INSERT INTO Employees (id, name, department, salary) VALUES (1, 'John', 'HR', 50000);

2. Write a query to insert multiple rows into a table in a single query.
  
Query:
INSERT INTO Employees (id, name, department, salary) 
VALUES
(2, 'Anush', 'IT', 40000),
(3, 'Raghav', 'production', 60000);

3. Write an INSERT query where only some columns are provided (not all).

Query:
INSERT INTO Employees (id, name, salary) 
VALUES
(4, 'Anush', 40000);

4. Write an INSERT query where only some columns are provided (not all).

Query:
UPDATE Employees
SET salary = 60000
WHERE id = 101;

5. Update the department of all employees from 'Sales' to 'Marketing'.

Query:
UPDATE Employees
SET department = 'Marketing'
WHERE department = 'Sales';

6. Increase salary by 10% for all employees in the 'IT' department.

Query:
UPDATE Employees
SET salary = salary * 1.10
WHERE department = 'IT';

7. Write a query to update multiple columns in a table using a single statement.

Query:
SET department = 'Engineer’, salary = salary * 1.15 WHERE department = 'IT';

8. Write a query to delete a record from the Employees table where ID = 5.

Query:
DELETE FROM Employees WHERE ID = 4;

9. Delete all employees whose department is m'HR'.

Query:
DELETE FROM Employees WHERE department = 'HR';

10. Delete all records from a table but keep the structure.

Query:
DELETE FROM Employees;

